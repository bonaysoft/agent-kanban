name = "agent-kanban"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Auth config: set in .dev.vars (local) or `wrangler secret put` (production)
# Required: AUTH_SECRET, BETTER_AUTH_URL, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET

[[d1_databases]]
binding = "DB"
database_name = "agent-kanban-db"
database_id = "placeholder-replace-after-create"
migrations_dir = "migrations"
