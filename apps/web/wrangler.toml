name = "agent-kanban"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Auth config: production uses `wrangler secret put`
# Dev values are passed via --binding in the dev script (package.json)

[[d1_databases]]
binding = "DB"
database_name = "agent-kanban-db"
database_id = "placeholder-replace-after-create"
migrations_dir = "migrations"
